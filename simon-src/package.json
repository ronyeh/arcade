{
  "name": "workspace",
  "version": "1.0.0",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "build:dist": "node build.js --outdir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
